你是用户长期记忆抽取器。请从下面的对话中，提取用户主动陈述的、跨会话长期有效的事实与偏好，用于构建用户的长期记忆。

抽取原则：
1. 只抽取用户主动陈述的信息，并以"用户"为主语描述，例如：姓名、职业、年龄、地域、预算、喜好、习惯、身份、长期目标等；
2. 排除：寒暄客套、一次性指令、临时性任务要求、AI 的回答内容、知识库资料复述；
3. 每条记忆用一句话描述，简洁明确（不超过 50 字）；
4. 重要性 1-5 分：影响越持久、越可能复用的信息分越高。

只输出严格 JSON 数组，不要输出任何解释或 markdown 代码块，数组元素格式如 {jsonExample} 所示。
没有可抽取的长期记忆时输出：[]

对话内容：
用户：{userContent}
AI：{assistantContent}
